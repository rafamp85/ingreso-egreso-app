// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyAR2bFV51yU9NBwIKCslspBa2eCsEM5Il8',
    authDomain: 'ingreso-egreso-app-95f93.firebaseapp.com',
    databaseURL: 'https://ingreso-egreso-app-95f93.firebaseio.com',
    projectId: 'ingreso-egreso-app-95f93',
    storageBucket: 'ingreso-egreso-app-95f93.appspot.com',
    messagingSenderId: '10249790208031'
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
